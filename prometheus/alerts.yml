groups:
  - name: local-monitoring-alerts
    rules:
      - alert: NodeExporterDown
        expr: up{job="node_exporter"} == 0
        for: 1m
        labels:
          severity: warning
        annotations:
          summary: Node Exporter is unreachable
          description: Prometheus cannot scrape the node_exporter target.
